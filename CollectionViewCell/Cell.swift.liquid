//
//  {{ module_info.name }}Cell.swift
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit
import SnapKit
import SQUIKit

#if DEBUG && canImport(SwiftUI)
import SwiftUI
#endif

final class {{ module_info.name }}Cell: UICollectionViewCell {

// MARK: - Model
    private var model: {{ module_info.name }}Model?

// MARK: - Inits
    override init(frame: CGRect) {
        super.init(frame: frame)

        self.configure()
        self.setupLayout()
    }
    
    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }
    
// MARK: - Bind
    @discardableResult
    func bind(model: {{ module_info.name }}Model) -> Self {
        self.model = model

        return self
    }
}

// MARK: - SQConfigurableView
extension {{ module_info.name }}Cell: SQConfigurableView {

    func configure() {
    }

    func setupLayout() {
    }
}

// MARK: - Preview
#if DEBUG
struct {{ module_info.name }}Cell_Previews: PreviewProvider {

    static var previews: some View {
        Group {
            UIViewPreview {
                {{ module_info.name }}Cell()
                    .bind(
                        model: {{ module_info.name }}Model(
                            id: "previewIdentifier"
                        )
                    )
            }
            .previewLayout(.fixed(width: 375, height: 100))
        }
    }
}
#endif
