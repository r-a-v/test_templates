//
//  {{ module_info.name }}View.swift
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit
import SQExtensions
import SnapKit
import SQUIKit

final class {{ module_info.name }}View: UICollectionReusableView {

// MARK: - Reusable identifier
    override var reuseIdentifier: String? {
        type(of: self).sq.identifier
    }

// MARK: - Model
    private var model: {{ module_info.name }}Model?

// MARK: - Inits
    override init(frame: CGRect) {
        super.init(frame: frame)

        self.configure()
        self.setupLayout()
    }

    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

// MARK: - Bind
    @discardableResult
    func bind(model: {{ module_info.name }}Model) -> Self {
        self.model = model

        return self
    }

}

// MARK: - SQConfigurableView
extension {{ module_info.name }}View: SQConfigurableView {

    func configure() {

    }

    func setupLayout() {

    }
}
